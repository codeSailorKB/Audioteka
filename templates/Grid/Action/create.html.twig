{% import 'MakeUiBundle:Macro:action_buttons.html.twig' as buttons %}

{% set parameters = {} %}
{% set label = options.parameters.message is defined ? options.parameters.message: '' %}

{% set parameters = options.parameters is defined ? parameters|merge(options.parameters) : parameters %}

{% if sylius_is_granted(grid.metadata.applicationName~'.'~grid.metadata.name~'.create') %}
    {{ buttons.create(path(grid.requestConfiguration.getRouteName('create'), parameters), label) }}
{% endif %}
